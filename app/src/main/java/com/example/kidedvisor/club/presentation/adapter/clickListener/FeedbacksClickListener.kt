package com.example.kidedvisor.club.presentation.adapter.clickListener

interface FeedbacksClickListener {
    fun onFeedbacksClick(feedback: String) // временная заглушка
    fun onMoreClick(feedback: String)
    fun onCommentClick(feedback: String)
}